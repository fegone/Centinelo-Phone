{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Centinelo Phone main window - custom titlebar controls + tray-driven show/hide + the auto-updater (updater:default) and its post-install relaunch (process:allow-restart, see shell/README.md \"Auto-updater\").",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "updater:default",
    "process:allow-restart"
  ]
}
